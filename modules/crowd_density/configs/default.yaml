data:
  raw_dir: "data/raw"
  dataset: "shanghaitech_part_b"
  image_size: 256
  downsample: 8      # matches model.py 3x maxpool (/8)
  sigma: 4

model:
  batch_size: 8
  epochs: 30
  learning_rate: 0.0001

density:
  capacity: 100       # default capacity for 100% density threshold calculation
